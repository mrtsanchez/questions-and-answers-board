
import { answersNumber } from 'questions-project/helpers/answers-number';
import { module, test } from 'qunit';

module('Unit | Helper | answers number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = answersNumber([42]);
  assert.ok(result);
});

