
import { questionsByCategory } from 'questions-project/helpers/questions-by-category';
import { module, test } from 'qunit';

module('Unit | Helper | questions by category');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionsByCategory([42]);
  assert.ok(result);
});

